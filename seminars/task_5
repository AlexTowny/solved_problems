#include <iostream>
#include <cmath>
using namespace std;



int main()
{

	int n;
	int count = 0;
	while (cin>>n){
		if(n == 0)
			break;
		count += ((n%2==0)?(0):(1));
	}
	cout << count;
    return 0;
}
